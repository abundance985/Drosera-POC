{
  "name": "honeypot-trap",
  "version": "1.0.0",
  "description": "Honeypot trap PoC for Drosera (Hardhat + ethers)",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run --network sepolia scripts/deploy.js"
  },
  "devDependencies": {
  "@nomicfoundation/hardhat-toolbox": "^3.0.0",
  "ethers": "^6.0.0",
  "hardhat": "^2.17.0",
  "dotenv": "^16.0.0"
}
}

